{
  "name": "video-convert",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node server.js",
    "build": "vite build",
    "preview": "npm run build && node server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
